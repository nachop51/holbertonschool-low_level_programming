#include "main.h"

/**
 * _sqrt_recursion - calculates a square root
 * @n: number
 *
 * Return: the square root of a number
 */
int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1);
	if (n == 1)
		return (1);

	
	return ();
}
