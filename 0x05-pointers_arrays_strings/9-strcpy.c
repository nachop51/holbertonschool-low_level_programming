#include <string.h>
#include "main.h"

/**
 * _strcpy - copy a string
 * @dest: dest
 * @src: source
 *
 * Return: a char
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
